#pragma once

// Compatibility umbrella: existing consumers keep the same include while the
// immutable data contract and concurrency boundary live in separate headers.
#include "measurement_snapshot_mailbox.h"
#include "measurement_snapshot_types.h"
